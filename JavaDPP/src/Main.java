import controller.Controller;

/** Entry point for the Dining Philosophers simulator. */
public class Main {

  /** Starts the interactive dining philosophers simulation. */
  public static void main(String[] args) {
    new Controller().run();
  }
}
